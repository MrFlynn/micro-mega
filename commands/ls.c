#include <string.h>

void ls_command() {
    for (uint8_t i = 0; i < num_files; i++) {
        for (uint8_t j = 0; i < strlen()) {
            strcat(disp_buffer, file_list[i][j]);
        }

        strcat(disp_buffer, " ");
    }
}